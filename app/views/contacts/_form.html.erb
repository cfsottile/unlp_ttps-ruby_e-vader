<%= form_for([@client, @contact]) do |f| %>
  <% if @contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% @contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :client_id, t("active_record.attributes.contact.client") %><br>
    <%= f.select :client_id, [[@client, @client.id]] %>
  </div>
  <div class="field">
    <%= f.label :kind, t("active_record.attributes.contact.kind") %><br>
    <%= f.select :kind, Contact.kinds.keys.map { |k| [t("active_record.attributes.contact.kinds.#{k}"), k]} %>
  </div>
  <div class="field">
    <%= f.label :value, t("active_record.attributes.contact.value") %><br>
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
